'use strict';

/**
 * @ngdoc function
 * @name dashboardApp.controller:ParticipantCtrl
 * @description
 * # ParticipantCtrl
 * Controller of the dashboardApp
 */
angular.module('dashboardApp')
  .controller('ParticipantCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
