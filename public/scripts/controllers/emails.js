'use strict';

/**
 * @ngdoc function
 * @name dashboardApp.controller:EmailsCtrl
 * @description
 * # EmailsCtrl
 * Controller of the dashboardApp
 */
angular.module('dashboardApp')
  .controller('EmailsCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
